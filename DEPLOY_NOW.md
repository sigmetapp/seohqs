# 🚀 Деплой прямо сейчас

## Вариант 1: Через Vercel CLI (самый быстрый)

```bash
# 1. Авторизуйтесь в Vercel
vercel login

# 2. Задеплойте проект
vercel --prod
```

При первом деплое Vercel спросит:
- Link to existing project? → **No** (создать новый)
- Project name → можете оставить по умолчанию
- Directory → **./** (текущая директория)

## Вариант 2: Через GitHub + Vercel Dashboard

1. **Закоммитьте и запушьте изменения:**
   ```bash
   git add .
   git commit -m "Setup Google Indexing Service"
   git push
   ```

2. **Перейдите на [vercel.com/new](https://vercel.com/new)**

3. **Импортируйте ваш репозиторий**

4. **Vercel автоматически определит настройки**

5. **Добавьте переменные окружения:**
   - Перейдите в Settings → Environment Variables
   - Добавьте:
     - `GOOGLE_SERVICE_ACCOUNT_EMAIL`
     - `GOOGLE_PRIVATE_KEY`
   - Выберите окружения: Production, Preview, Development

6. **Нажмите Deploy**

## Вариант 3: Автоматический деплой через GitHub Actions

Если хотите автоматический деплой при каждом пуше:

1. **Создайте токены в Vercel:**
   - Перейдите на [vercel.com/account/tokens](https://vercel.com/account/tokens)
   - Создайте новый токен
   - Скопируйте токен

2. **Добавьте секреты в GitHub:**
   - Перейдите в Settings → Secrets and variables → Actions
   - Добавьте:
     - `VERCEL_TOKEN` = ваш токен из шага 1
     - `VERCEL_ORG_ID` = можно получить через `vercel whoami` или в настройках проекта
     - `VERCEL_PROJECT_ID` = можно получить после первого деплоя или в настройках проекта

3. **Запушьте код:**
   ```bash
   git add .
   git commit -m "Add GitHub Actions workflow"
   git push
   ```

4. **GitHub Actions автоматически задеплоит проект**

## Получение VERCEL_ORG_ID и VERCEL_PROJECT_ID

После первого деплоя через CLI или Dashboard:

```bash
# Получить информацию о проекте
vercel inspect

# Или посмотрите в файле .vercel/project.json после первого деплоя
```

## Быстрая команда для деплоя

Если уже авторизованы в Vercel:

```bash
vercel --prod --yes
```

---

**После деплоя не забудьте добавить переменные окружения в настройках проекта на Vercel!**
