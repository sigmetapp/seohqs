# Запросы к Google Search Console API

## Обзор

Когда пользователь выбирает фильтры **90 дней** или **180 дней** на странице `/dashboard-gc`, сайт **НЕ делает прямые запросы к Google Console API**. Вместо этого данные загружаются из локальной базы данных.

Запросы к Google Search Console API выполняются **только при синхронизации** (кнопка "🔄 Синхронизировать").

---

## 1. Выбор фильтра 90 или 180 дней (без синхронизации)

### Frontend запрос
```
GET /api/sites/{siteId}/google-console/daily?days=90
GET /api/sites/{siteId}/google-console/daily?days=180
```

### Что происходит
- Endpoint загружает данные **из локальной БД** (не из Google Console API)
- Всегда загружает данные за последние **180 дней** из БД, затем фильтрует по запрошенному периоду
- **НЕ делает запрос к Google Console API**

### Пример запроса к БД
```sql
SELECT * FROM google_search_console_data 
WHERE site_id = {siteId} 
AND date >= '{startDate}' 
AND date <= '{endDate}' 
ORDER BY date ASC 
LIMIT 2000
```

Где:
- `startDate` = сегодня - 180 дней (для загрузки из БД)
- `endDate` = сегодня
- Затем данные фильтруются по запрошенному периоду (90 или 180 дней)

---

## 2. Синхронизация данных (запрос к Google Console API)

### Frontend запрос
```
POST /api/sites/{siteId}/google-console/sync
```

### Что происходит
1. Endpoint вызывает `searchConsoleService.getAggregatedData(..., 180)`
2. Всегда запрашивает данные за **180 дней** (независимо от выбранного фильтра)
3. Делает запрос к Google Search Console API

---

## 3. Запрос к Google Search Console API

### Endpoint
```
POST https://searchconsole.googleapis.com/webmasters/v3/sites/{siteUrl}/searchAnalytics/query
```

### Headers
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

### Request Body для 90 дней (если бы делался запрос)

**НЕ используется в текущей реализации**, но если бы использовался:

```json
{
  "startDate": "2025-08-21",
  "endDate": "2025-11-19",
  "dimensions": ["date"],
  "rowLimit": 25000
}
```

Где:
- `startDate` = сегодня - 90 дней (формат: YYYY-MM-DD)
- `endDate` = сегодня (формат: YYYY-MM-DD)
- `dimensions` = `["date"]` (только группировка по дате)
- `rowLimit` = `25000` (максимальное значение, разрешенное API)

---

### Request Body для 180 дней (при синхронизации)

**Используется при синхронизации:**

```json
{
  "startDate": "2025-05-23",
  "endDate": "2025-11-19",
  "dimensions": ["date"],
  "rowLimit": 25000
}
```

Где:
- `startDate` = сегодня - 180 дней (формат: YYYY-MM-DD)
- `endDate` = сегодня (формат: YYYY-MM-DD)
- `dimensions` = `["date"]` (только группировка по дате)
- `rowLimit` = `25000` (максимальное значение, разрешенное API)

### Пример полного запроса (cURL)

```bash
curl -X POST \
  "https://searchconsole.googleapis.com/webmasters/v3/sites/sc-domain:example.com/searchAnalytics/query" \
  -H "Authorization: Bearer {access_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "startDate": "2025-05-23",
    "endDate": "2025-11-19",
    "dimensions": ["date"],
    "rowLimit": 25000
  }'
```

### Response от Google Console API

```json
{
  "responseAggregationType": "byDate",
  "rows": [
    {
      "keys": ["2025-05-23"],
      "clicks": 10,
      "impressions": 100,
      "ctr": 0.1,
      "position": 5.5
    },
    {
      "keys": ["2025-05-24"],
      "clicks": 12,
      "impressions": 120,
      "ctr": 0.1,
      "position": 5.3
    }
    // ... до 180 записей (по одной на день)
  ]
}
```

---

## 4. Важные замечания

### Почему всегда запрашивается 180 дней при синхронизации?

1. **Эффективность**: Один запрос к API вместо нескольких
2. **Кеширование**: Данные сохраняются в БД за максимальный период
3. **Гибкость**: Фильтры 7, 30, 90, 180 дней работают без дополнительных запросов к API

### Ограничения Google Search Console API

- Максимальный период: **16 месяцев** (около 480 дней)
- Максимальный `rowLimit`: **25000**
- Минимальный период: **1 день**
- Данные доступны с задержкой: **2-3 дня**

### Логирование

Все запросы к Google Console API логируются в консоль:

```javascript
console.log('[GSC API] Request:', {
  siteUrl: normalizedSiteUrl,
  startDate,
  endDate,
  dimensions: requestDimensions,
  rowLimit: requestBody.rowLimit,
});
```

---

## 5. Схема работы

```
┌─────────────────┐
│  Dashboard GC   │
│  (Frontend)     │
└────────┬────────┘
         │
         │ Выбор фильтра 90/180 дней
         ▼
┌─────────────────────────────┐
│ GET /api/sites/{id}/        │
│ google-console/daily?days=  │
│ {90|180}                    │
└────────┬────────────────────┘
         │
         │ Загрузка из БД
         ▼
┌─────────────────────────────┐
│  Local Database             │
│  (google_search_console_   │
│   data table)               │
└─────────────────────────────┘

┌─────────────────┐
│  Dashboard GC   │
│  (Frontend)     │
└────────┬────────┘
         │
         │ Кнопка "Синхронизировать"
         ▼
┌─────────────────────────────┐
│ POST /api/sites/{id}/       │
│ google-console/sync         │
└────────┬────────────────────┘
         │
         │ getAggregatedData(180)
         ▼
┌─────────────────────────────┐
│  Google Search Console API  │
│  POST /searchAnalytics/query │
│  {startDate, endDate,       │
│   dimensions: ["date"],     │
│   rowLimit: 25000}          │
└────────┬────────────────────┘
         │
         │ Response
         ▼
┌─────────────────────────────┐
│  Local Database             │
│  (сохранение данных)        │
└─────────────────────────────┘
```

---

## 6. Важно: Почему данные могут показываться только за 30 дней

### Проблема
Если при выборе фильтра 90 или 180 дней данные показываются только за последние 30 дней, это может быть связано с тем, что **Google Console API возвращает данные только за 30 дней**.

### Причины
1. **Сайт был недавно добавлен в Google Search Console** - для новых сайтов данные могут быть доступны только за последние 30 дней
2. **Ограниченная история данных** - Google Console может иметь ограничения на доступность данных для некоторых сайтов
3. **Задержка обновления данных** - Google Console обновляет данные с задержкой в 2-3 дня

### Как проверить
После синхронизации проверьте логи сервера:
```
[GSC Sync] Received {count} records from Google Console API
[GSC Sync] Actual date range from API: {firstDate} - {lastDate} ({actualDays} days)
```

Если `{actualDays}` меньше 150, значит Google Console API вернул меньше данных, чем запрошено.

### Решение
1. Подождите несколько дней - Google Console может обновить данные
2. Проверьте в Google Search Console напрямую, какие данные доступны
3. Убедитесь, что сайт был добавлен в Google Search Console достаточно давно

Подробнее см. файл `DEBUG_30_DAYS_ISSUE.md`

---

## 7. Выводы

1. **Фильтры 90/180 дней** не делают запросы к Google Console API напрямую
2. **Синхронизация** всегда запрашивает данные за **180 дней**
3. Данные сохраняются в БД и используются для всех фильтров
4. Google Console API вызывается только при явной синхронизации
5. **Если данные показываются только за 30 дней**, проверьте логи синхронизации - возможно, Google Console API возвращает только 30 дней данных
