# Проблема: Данные показываются только за 30 дней вместо 90/180

## Описание проблемы

При выборе фильтров **90 дней** или **180 дней** на странице `/dashboard-gc`, данные показываются только за последние **30 дней**.

## Возможные причины

### 1. Google Console API возвращает данные только за 30 дней

**Самая вероятная причина**: Google Search Console API может возвращать данные только за последние 30 дней, если:
- Сайт был недавно добавлен в Google Search Console
- Данные не были доступны раньше (Google Console имеет задержку в 2-3 дня)
- Сайт не имеет достаточной истории данных

**Как проверить:**
1. Запустите синхронизацию для сайта (кнопка "🔄 Синхронизировать")
2. Проверьте логи сервера - должны появиться сообщения:
   ```
   [GSC Sync] Requesting data for site {id} ({domain}):
   [GSC Sync] Expected date range: {startDate} - {endDate} (180 days)
   [GSC getAggregatedData] Requesting 180 days of data for site: {siteUrl}
   [GSC API] Expected days in range: 180 days
   [GSC API] Response: {count} rows returned
   [GSC API] Response date range: {firstDate} - {lastDate} ({actualDays} days)
   [GSC Sync] Received {count} records from Google Console API
   [GSC Sync] Actual date range from API: {firstDate} - {lastDate} ({actualDays} days)
   ```

3. Если в логах видно, что API возвращает только ~30 дней данных, значит проблема в Google Console API

### 2. Проблема с сохранением данных в БД

**Возможная причина**: Данные могут сохраняться неправильно или перезаписываться.

**Как проверить:**
1. После синхронизации проверьте БД напрямую:
   ```sql
   SELECT 
     COUNT(*) as total_records,
     MIN(date) as first_date,
     MAX(date) as last_date,
     COUNT(DISTINCT date) as unique_dates
   FROM google_search_console_data
   WHERE site_id = {siteId};
   ```

2. Если в БД есть данные только за 30 дней, значит проблема в синхронизации или в том, что API возвращает только 30 дней

### 3. Проблема с загрузкой данных из БД

**Менее вероятная причина**: При загрузке данных из БД может использоваться неправильный фильтр.

**Как проверить:**
1. Проверьте логи при загрузке данных:
   ```
   [Google Console Daily] Loading data from DB for site {id}, date range: {startDate} - {endDate}, limit: {limit}
   [Google Console Daily] Loaded {count} records from DB for site {id}
   [Google Console Daily] Site {id}, Period: {days} days, Base records: {baseCount}, Filtered: {filteredCount} records
   [Google Console Daily] Requested date range: {startDate} - {endDate}
   [Google Console Daily] Actual data range: {firstDate} - {lastDate} ({actualDays} days)
   ```

2. Если в БД есть данные за 180 дней, но загружается только 30, значит проблема в фильтрации

## Диагностика

### Шаг 1: Проверьте логи синхронизации

После нажатия кнопки "🔄 Синхронизировать" проверьте логи сервера. Должны быть видны:

1. **Запрос к Google Console API:**
   ```
   [GSC API] Request: {
     siteUrl: "...",
     startDate: "2025-05-23",
     endDate: "2025-11-19",
     dimensions: ["date"],
     rowLimit: 25000
   }
   [GSC API] Expected days in range: 180 days
   ```

2. **Ответ от Google Console API:**
   ```
   [GSC API] Response: {count} rows returned
   [GSC API] Response date range: {firstDate} - {lastDate} ({actualDays} days)
   ```

3. **Результаты синхронизации:**
   ```
   [GSC Sync] Received {count} records from Google Console API
   [GSC Sync] Actual date range from API: {firstDate} - {lastDate} ({actualDays} days)
   ```

### Шаг 2: Проверьте данные в БД

Выполните SQL запрос для проверки данных в БД:

```sql
-- Проверка количества записей и диапазона дат
SELECT 
  site_id,
  COUNT(*) as total_records,
  MIN(date) as first_date,
  MAX(date) as last_date,
  COUNT(DISTINCT date) as unique_dates,
  EXTRACT(EPOCH FROM (MAX(date)::date - MIN(date)::date)) / 86400 as days_span
FROM google_search_console_data
WHERE site_id = {siteId}
GROUP BY site_id;
```

### Шаг 3: Проверьте загрузку данных

При выборе фильтра 90 или 180 дней проверьте логи:

```
[Google Console Daily] Loading data from DB for site {id}, date range: {startDate} - {endDate}, limit: {limit}
[Google Console Daily] Loaded {count} records from DB for site {id}
[Google Console Daily] Site {id}, Period: {days} days, Base records: {baseCount}, Filtered: {filteredCount} records
[Google Console Daily] Requested date range: {startDate} - {endDate}
[Google Console Daily] Actual data range: {firstDate} - {lastDate} ({actualDays} days)
```

## Решения

### Решение 1: Если Google Console API возвращает только 30 дней

**Причина**: Google Console API имеет ограничения на доступность данных для новых сайтов или сайтов с ограниченной историей.

**Что делать:**
1. Подождите несколько дней - Google Console может обновить данные
2. Проверьте в Google Search Console напрямую, какие данные доступны
3. Убедитесь, что сайт был добавлен в Google Search Console достаточно давно

### Решение 2: Если данные в БД только за 30 дней

**Причина**: При синхронизации сохраняются только данные за 30 дней.

**Что делать:**
1. Проверьте логи синхронизации - сколько записей получено от API
2. Если API возвращает только 30 дней, см. Решение 1
3. Если API возвращает 180 дней, но в БД только 30, проверьте функцию `bulkInsertGoogleSearchConsoleData`

### Решение 3: Если данные в БД есть за 180 дней, но загружается только 30

**Причина**: Проблема в фильтрации данных при загрузке из БД.

**Что делать:**
1. Проверьте параметр `days` в запросе к `/api/sites/{id}/google-console/daily?days={90|180}`
2. Проверьте логи загрузки данных
3. Убедитесь, что фильтрация по дате работает правильно

## Добавленное логирование

Для диагностики проблемы добавлено подробное логирование:

1. **В функции синхронизации** (`/app/api/sites/[id]/google-console/sync/route.ts`):
   - Логирование ожидаемого диапазона дат
   - Логирование фактического диапазона дат из API
   - Предупреждение, если получено меньше данных, чем запрошено

2. **В функции getAggregatedData** (`/lib/google-search-console.ts`):
   - Логирование запрашиваемого периода
   - Логирование количества полученных строк

3. **В функции getPerformanceData** (`/lib/google-search-console.ts`):
   - Логирование ожидаемого количества дней
   - Логирование фактического диапазона дат в ответе
   - Предупреждение, если получено меньше данных, чем запрошено

## Следующие шаги

1. Запустите синхронизацию для проблемного сайта
2. Проверьте логи сервера
3. Определите, на каком этапе теряются данные:
   - При запросе к Google Console API?
   - При сохранении в БД?
   - При загрузке из БД?
4. Примените соответствующее решение

## Дополнительная информация

- Google Search Console API поддерживает данные за последние **16 месяцев** (около 480 дней)
- Минимальный период для запроса: **1 день**
- Данные доступны с задержкой: **2-3 дня**
- Для новых сайтов данные могут быть доступны только за последние 30 дней
