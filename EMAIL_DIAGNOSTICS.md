# Диагностика проблемы с отправкой email для сброса пароля

Если письма для сброса пароля не приходят на почту, выполните следующие шаги диагностики:

## Быстрая диагностика

### 1. Проверьте настройки через API

Откройте в браузере или выполните запрос:
```
GET /api/admin/email-diagnostics
```

Этот endpoint покажет:
- Какие методы отправки email настроены
- Какие переменные окружения установлены
- Какой метод будет использован
- Рекомендации по настройке

### 2. Проверьте логи сервера

При запросе сброса пароля в логах должны появиться сообщения:

**Успешная отправка:**
```
✅ Email для сброса пароля отправлен на user@example.com
```

**Ошибка отправки:**
```
❌ Ошибка отправки email: [детали ошибки]
📋 Текущие настройки email:
  - RESEND_API_KEY: ✅ установлен / ❌ не установлен
  - SUPABASE_SMTP_HOST: ...
  - SMTP_HOST: ...
```

### 3. Тестовая отправка email

Отправьте POST запрос для проверки:
```bash
curl -X POST http://localhost:3000/api/admin/email-diagnostics \
  -H "Content-Type: application/json" \
  -d '{"email": "ваш@email.com"}'
```

Или через браузер с расширением для API (например, REST Client в VS Code):
```json
POST /api/admin/email-diagnostics
Content-Type: application/json

{
  "email": "ваш@email.com"
}
```

## Пошаговая диагностика

### Шаг 1: Проверка переменных окружения

Убедитесь, что установлены необходимые переменные окружения:

#### Для Resend API (рекомендуется):
```env
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@yourdomain.com
NEXT_PUBLIC_APP_URL=https://yourdomain.com
```

#### Для Supabase SMTP:
```env
SUPABASE_SMTP_HOST=smtp.gmail.com
SUPABASE_SMTP_PORT=587
SUPABASE_SMTP_USER=your-email@gmail.com
SUPABASE_SMTP_PASSWORD=your-app-password
SUPABASE_SMTP_FROM=noreply@yourdomain.com
NEXT_PUBLIC_APP_URL=https://yourdomain.com
```

#### Для обычного SMTP:
```env
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-password
SMTP_FROM=noreply@yourdomain.com
NEXT_PUBLIC_APP_URL=https://yourdomain.com
```

### Шаг 2: Проверка в Vercel (если используется)

1. Откройте Vercel Dashboard
2. Выберите ваш проект
3. Перейдите в Settings → Environment Variables
4. Убедитесь, что все переменные установлены
5. **Важно:** После добавления переменных нужно перезапустить деплоймент

### Шаг 3: Проверка логов в Vercel

1. Откройте Vercel Dashboard
2. Выберите ваш проект
3. Перейдите в Deployments → выберите последний деплоймент
4. Откройте вкладку "Functions" или "Logs"
5. Найдите логи, связанные с `/api/auth/user/forgot-password`

### Шаг 4: Проверка работы email провайдера

#### Для Resend:
1. Откройте https://resend.com/dashboard
2. Проверьте вкладку "Logs" - там должны быть записи о попытках отправки
3. Проверьте статус домена (если используете свой домен)
4. Проверьте лимиты отправки

#### Для Gmail SMTP:
1. Убедитесь, что используете "Пароль приложения", а не обычный пароль
2. Создайте пароль приложения: https://myaccount.google.com/apppasswords
3. Включите двухфакторную аутентификацию в Google аккаунте

#### Для других SMTP провайдеров:
1. Проверьте правильность хоста и порта
2. Проверьте учетные данные
3. Убедитесь, что SMTP сервер не блокирует подключения

## Частые проблемы и решения

### Проблема 1: "Email не настроен"

**Симптомы:** В логах видно "⚠️ Email не отправлен (режим разработки)"

**Решение:**
1. Установите переменные окружения для одного из методов отправки
2. Перезапустите сервер/деплоймент
3. Проверьте через `/api/admin/email-diagnostics`

### Проблема 2: "Ошибка отправки через Resend"

**Симптомы:** В логах видно "❌ Ошибка отправки через Resend"

**Возможные причины:**
- Неверный API ключ
- Домен не верифицирован (если используете свой домен)
- Превышен лимит отправки

**Решение:**
1. Проверьте API ключ в Resend Dashboard
2. Если используете свой домен, убедитесь, что он верифицирован
3. Проверьте лимиты в Resend Dashboard
4. Попробуйте использовать `onboarding@resend.dev` как временный адрес отправителя

### Проблема 3: "Ошибка отправки через SMTP"

**Симптомы:** В логах видно "❌ Ошибка отправки через SMTP"

**Возможные причины:**
- Неверные учетные данные
- Неправильный порт (587 для TLS, 465 для SSL)
- SMTP сервер блокирует подключение
- Для Gmail: используется обычный пароль вместо пароля приложения

**Решение:**
1. Проверьте учетные данные
2. Для Gmail: используйте пароль приложения
3. Проверьте, что порт правильный (587 или 465)
4. Проверьте настройки безопасности SMTP сервера

### Проблема 4: "Письмо не приходит, но ошибок нет"

**Симптомы:** В логах видно "✅ Email успешно отправлен", но письмо не приходит

**Возможные причины:**
- Письмо попало в спам
- Неверный email адрес пользователя
- Проблемы на стороне email провайдера получателя

**Решение:**
1. Проверьте папку "Спам"
2. Проверьте правильность email адреса
3. Проверьте логи email провайдера (Resend Dashboard, SendGrid и т.д.)
4. Попробуйте отправить тестовое письмо через `/api/admin/email-diagnostics`

### Проблема 5: "Неправильная ссылка в письме"

**Симптомы:** Письмо приходит, но ссылка ведет не туда

**Решение:**
1. Убедитесь, что установлена переменная `NEXT_PUBLIC_APP_URL`
2. В production используйте полный URL с протоколом: `https://yourdomain.com`
3. Проверьте через `/api/admin/email-diagnostics`, что `appUrl` правильный

## Проверка через код

Если нужно проверить настройки программно:

```typescript
// Проверка переменных окружения
console.log('RESEND_API_KEY:', process.env.RESEND_API_KEY ? 'установлен' : 'не установлен');
console.log('SUPABASE_SMTP_HOST:', process.env.SUPABASE_SMTP_HOST || 'не установлен');
console.log('SMTP_HOST:', process.env.SMTP_HOST || 'не установлен');
console.log('NEXT_PUBLIC_APP_URL:', process.env.NEXT_PUBLIC_APP_URL || 'не установлен');
```

## Рекомендации

1. **Для production:** Используйте Resend API или Supabase SMTP
2. **Для разработки:** Можно оставить без настройки - ссылка будет выводиться в консоль
3. **Мониторинг:** Регулярно проверяйте логи отправки в вашем email провайдере
4. **Тестирование:** Используйте `/api/admin/email-diagnostics` для тестовой отправки перед использованием в production

## Дополнительная помощь

Если проблема не решена:
1. Проверьте полные логи сервера
2. Используйте `/api/admin/email-diagnostics` для диагностики
3. Проверьте документацию вашего email провайдера
4. Убедитесь, что все переменные окружения установлены и доступны в runtime
