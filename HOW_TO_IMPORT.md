# 📥 Как импортировать проект в Vercel

## Пошаговая инструкция

### Шаг 1: Откройте Vercel Dashboard

Перейдите на: **https://vercel.com/new**

### Шаг 2: Авторизуйтесь через GitHub

1. Нажмите кнопку **"Continue with GitHub"** или **"Import Git Repository"**
2. Разрешите Vercel доступ к вашему GitHub аккаунту
3. Выберите организацию (если нужно)

### Шаг 3: Выберите репозиторий

1. В списке репозиториев найдите: **`sigmetapp/seohqs`**
2. Нажмите **"Import"**

### Шаг 4: Настройте проект

Vercel автоматически определит Next.js, но проверьте настройки:

- **Framework Preset:** Next.js (должно быть автоматически)
- **Root Directory:** `./` (оставьте по умолчанию)
- **Build Command:** `npm run build` (должно быть автоматически)
- **Output Directory:** `.next` (должно быть автоматически)
- **Install Command:** `npm install` (должно быть автоматически)

### Шаг 5: Настройте переменные окружения

**⚠️ ВАЖНО: Сделайте это ДО деплоя!**

1. В разделе **"Environment Variables"** нажмите **"Add"**
2. Добавьте первую переменную:
   - **Name:** `GOOGLE_SERVICE_ACCOUNT_EMAIL`
   - **Value:** ваш email Service Account (из JSON файла, поле `client_email`)
   - **Environment:** выберите все (Production, Preview, Development)
3. Добавьте вторую переменную:
   - **Name:** `GOOGLE_PRIVATE_KEY`
   - **Value:** ваш приватный ключ (из JSON файла, поле `private_key`)
     - Можно вставить многострочный ключ - Vercel обработает его автоматически
     - Или в одну строку с `\n`: `"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"`
   - **Environment:** выберите все (Production, Preview, Development)

### Шаг 6: Выберите ветку для деплоя

- **Production Branch:** `main` или `master` (или ваша основная ветка)
- Можно оставить текущую ветку для первого деплоя

### Шаг 7: Деплой

1. Нажмите кнопку **"Deploy"**
2. Дождитесь завершения сборки (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL: `https://your-project.vercel.app`

## 🎯 Настройка Preview Deploys (Превью деплоев)

### Автоматические превью деплои

После импорта Vercel **автоматически** создаст превью деплои для:
- ✅ Pull Requests (PR)
- ✅ Ветки, отличные от production ветки
- ✅ Коммиты в feature ветках

### Как проверить, что превью работают:

1. **Создайте Pull Request:**
   ```bash
   # Создайте новую ветку
   git checkout -b test-preview
   
   # Сделайте небольшое изменение
   echo "# Test" >> test.md
   
   # Закоммитьте и запушьте
   git add test.md
   git commit -m "Test preview deploy"
   git push origin test-preview
   ```

2. **Создайте PR на GitHub:**
   - Перейдите на GitHub в ваш репозиторий
   - Нажмите "New Pull Request"
   - Выберите ветку `test-preview` → `main`
   - Создайте PR

3. **Проверьте Vercel:**
   - В Vercel Dashboard откройте ваш проект
   - Перейдите в раздел **"Deployments"**
   - Вы увидите новый превью деплой с комментарием от бота GitHub

### Настройка превью деплоев вручную

Если превью не создаются автоматически:

1. Перейдите в **Settings** → **Git**
2. Убедитесь, что:
   - ✅ **"Automatic deployments from Git"** включено
   - ✅ **"Preview deployments"** включено
   - ✅ GitHub интеграция подключена

3. В разделе **"Deployment Protection"**:
   - Можно настроить, какие ветки создают превью
   - По умолчанию: все ветки кроме production

## 🔍 Проверка деплоя

### Production деплой:
- URL: `https://your-project.vercel.app`
- Создается при пуше в `main`/`master` ветку

### Preview деплой:
- URL: `https://your-project-git-branch-name.vercel.app`
- Создается автоматически для PR и других веток
- Комментарий в PR с ссылкой на превью

## ❓ Почему нет превью деплоя?

### Возможные причины:

1. **Проект еще не импортирован в Vercel**
   - Решение: импортируйте проект (см. инструкцию выше)

2. **GitHub интеграция не подключена**
   - Решение: Settings → Git → Connect GitHub

3. **Превью деплои отключены**
   - Решение: Settings → Git → включите "Preview deployments"

4. **Нет Pull Request**
   - Решение: создайте PR для тестирования превью

5. **Ветка совпадает с production веткой**
   - Решение: используйте другую ветку для превью

## 🚀 Быстрый тест превью деплоя

```bash
# 1. Создайте тестовую ветку
git checkout -b test-preview-deploy

# 2. Сделайте небольшое изменение
echo "Test preview" > test.txt
git add test.txt
git commit -m "Test preview deploy"
git push origin test-preview-deploy

# 3. Создайте PR на GitHub
# 4. Проверьте Vercel Dashboard - должен появиться превью деплой
```

## 📝 Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation - Preview Deployments](https://vercel.com/docs/concepts/deployments/preview-deployments)
- [GitHub Integration](https://vercel.com/docs/concepts/git)
